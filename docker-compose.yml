version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: miboleta_app
    tty: true
    working_dir: /var/www/html
    environment:
      SERVICE_NAME: app
      SERVICE_TAGS: dev
    volumes:
      - ./backend:/var/www/html
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    depends_on:
      - db
    networks:
      - miboleta_network

  nginx:
    image: nginx:alpine
    container_name: miboleta_nginx
    tty: true
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./backend/public:/var/www/html/public:ro
      - ./backend/storage/app/public:/var/www/html/public/storage:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - miboleta_network

  db:
    image: mysql:8.0
    container_name: miboleta_mysql
    tty: true
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: miboleta
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    cap_add:
      - SYS_NICE  # CAP_SYS_NICE
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - miboleta_network

  adminer:
    image: adminer:latest
    command: [ "php", "-d", "memory_limit=4G", "-d", "upload_max_filesize=10G", "-d", "post_max_size=10G", "-S", "[::]:8080", "-t", "/var/www/html" ]
    container_name: miboleta_adminer
    ports:
      - "8080:8080"
    environment:
      - ADMINER_DEFAULT_SERVER=db
    depends_on:
      - db
    networks:
      - miboleta_network

volumes:
  mysql_data:
    driver: local

networks:
  miboleta_network:
    driver: bridge
